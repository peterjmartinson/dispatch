Add unit tests for both print_watcher and email_watcher scripts. Tests should check handling of new files, repeated files, file locking scenarios, and logging output. Mock external dependencies (`lp`, SMTP server). Include setup for pytest.

- Use a tests/ directory
- README section on how to run tests
- Example pytest command

